import {StyleSheet, Text, View} from 'react-native';
import {PAINTINGS} from '../data';

const PaintersList = () => {
  return (
    <View>
      <Text>PaintersList</Text>
    </View>
  );
};

export default PaintersList;

const styles = StyleSheet.create({});
